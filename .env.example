# ============================================================
# Medical Aesthetics RAG — 环境变量配置参考
# 复制为 .env 并按需修改，未设置的变量使用括号内的默认值
# ============================================================

# ===== LLM / OpenAI 配置 =====
# OPENAI_API_KEY=sk-xxx                # OpenAI/兼容 API 密钥
# OPENAI_API_BASE=                      # API 地址（留空使用官方）
# OPENAI_MODEL=gpt-4o-mini             # 对话模型名称
# LLM_CLIENT_TIMEOUT=60                # LLM 请求超时秒数 (60)

# ===== 安全 =====
# ADMIN_API_KEY=                        # 管理接口密钥（留空则不鉴权）
# CORS_ORIGINS=*                        # CORS 允许源（生产环境设为具体域名，逗号分隔）

# ===== 限流 =====
# ASK_RATE_LIMIT=60/minute              # /ask 端点限流 (60/minute)
# ADMIN_RATE_LIMIT=30/minute            # 管理端点限流 (30/minute)

# ===== 请求控制 =====
# ASK_TIMEOUT_SEC=30                    # /ask 超时秒数 (30)
# MAX_QUESTION_LEN=500                  # 问题最大长度 (500)
# MAX_HISTORY_TOTAL_CHARS=3000          # 历史消息最大字符数 (3000)

# ===== 缓存 =====
# RESPONSE_CACHE_MAX=200                # 响应缓存最大条数 (200)
# RESPONSE_CACHE_TTL=300                # 响应缓存 TTL 秒数 (300)
# RAG_EMBED_CACHE_MAX=1024              # 嵌入向量缓存条数 (1024)
# RAG_KNOWLEDGE_CACHE_MAX=128           # 知识文件缓存条数 (128)
# RAG_SHARED_CACHE_MAX=32               # 共享知识缓存条数 (32)
# RAG_JIEBA_CACHE_MAX=256               # jieba 分词缓存条数 (256)
# RAG_RERANK_CACHE_MAX=512              # rerank 缓存条数 (512)
# HEALTH_CACHE_TTL=15.0                 # 健康检查缓存秒数 (15.0)

# ===== 嵌入模型 =====
# EMBED_MODEL_NAME=BAAI/bge-m3          # 嵌入模型路径/名称
# EMBED_USE_FP16=true                   # 是否使用 FP16 (true)

# ===== 日志 =====
# LOG_MAX_BYTES=10485760                # 日志文件大小上限字节 (10MB)
# LOG_BACKUP_COUNT=3                    # 日志轮转保留数量 (3)

# ===== 启动 =====
# SKIP_WARMUP=                          # 设置任意值跳过启动预热
# OPENAI_COMPAT_MODEL=medical-rag       # OpenAI 兼容端点模型名 (medical-rag)
